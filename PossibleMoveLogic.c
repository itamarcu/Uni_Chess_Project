
#include <stdlib.h>
#include "PossibleMoveLogic.h"


PossibleMove *get_possible_moves(Game *game, int r1, int c1) {
    PossibleMove *get_possible_moves = (PossibleMove *) malloc(32 * sizeof(PossibleMove));

    //size is 32, remember!

    for (int i = 0; i < 32; i++) {

    }

}